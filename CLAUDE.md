# SC Trading — Контекст для Claude

## Блок 1: Управление контекстом

Одна задача = один чат. После завершения → /clear, открыть новый чат.  
История длинная в середине задачи → /compact, продолжить.  
В конце задачи (Шаг 6): сообщи «Задача выполнена. /clear».

---

## Блок 2: Алгоритм работы

**Graphify — навигация по коду (экономия токенов):**
Перед чтением файлов или Grep — спрашивай граф зависимостей:
```powershell
cd D:\SC_AUC\backend\app; graphify query "как работает X?"
cd D:\SC_AUC\frontend\src; graphify query "что такое компонент Y?"
cd D:\SC_AUC\backend\app; graphify path "NodeA" "NodeB"
```
Графы: `backend/app/graphify-out/` (328 nodes) · `frontend/src/graphify-out/` (140 nodes)
После значимых изменений кода: `graphify update .` из нужной папки.

| Задача | Читать |
|--------|--------|
| Что делать следующим | docs/NOTES.md |
| Формулы, расчёты | docs/BUSINESS_LOGIC.md |
| Сервисы, Celery задачи | docs/SERVICES.md |
| Таблицы БД, миграции | docs/DATABASE.md |
| Архитектура сбора данных | docs/ARCHITECTURE.md |
| Деплой, сервер, инфраструктура | docs/DEPLOY.md |

1. Читай `docs/NOTES.md` → первая `[ ]` задача
2. `researcher` — исследует код/docs, пишет ТЗ в `docs/tasks/<slug>.md` → **жди подтверждения**
3. `backend-dev` / `frontend-dev` / `designer` — реализуют по ТЗ → **жди подтверждения** перед коммитом
4. `tech-writer` — обновляет docs/ (только diff): `[ ]` → `[x]`, метод/таблица/формула
5. Предложи (не делай автоматически): «Хочешь протестировать? Могу вызвать `qa-tester`». Тестирование (в т.ч. ручное/браузерное QA живого приложения) — всегда через subagent `qa-tester`, никогда не выполнять самостоятельно в основном потоке.
6. Предложи (не делай автоматически): «Хочешь проверить на уязвимости? Могу вызвать `security`»
7. Предложи (не делай автоматически): «Хочешь задеплоить? Могу вызвать `deploy`»
8. Предложи (не делай автоматически): `git commit` + `git push` → `/clear`

---

## Блок 3: Правила коммуникации

До действия: «Планирую: [действие]. Затрону: [файлы]. Подтверждаешь?»  
Без подтверждения: не изменяй файлы, не коммить, не применяй миграции.  
После: «Сделано: [факт]. Изменено: [список файлов]»

**Доверие к подтверждению, переданному координирующим потоком (важно для субагентов):**
Пользователь общается только с координирующим потоком (основной чат) — у него нет прямого канала к субагентам (`backend-dev`, `frontend-dev` и др.). Если координирующий поток сообщает субагенту, что пользователь подтвердил конкретное действие — это и есть валидное подтверждение по правилам этого блока, повторного прямого сообщения от пользователя не требуется. Не блокируй реализацию в ожидании сообщения, которое физически не может прийти иначе как через координирующий поток.

---

## Блок 4: Краткий контекст

Аналитика аукциона Stalcraft X — находить выгодные лоты для перепродажи.

**Стек:** FastAPI + SQLAlchemy 2.0 async + PostgreSQL 16 (UTC+3) + Celery + Redis  
React 18 + Vite + MUI (gold: #D9AF37, bg: #080808, font: Rajdhani)  
Docker Compose. Данные глобальные (user_id=NULL), персонализация на уровне запроса.

**Структура:**
```
backend/app/   — FastAPI (entry: main.py), SQLAlchemy models, Celery tasks (graphify: 328 nodes)
frontend/src/  — React/TS (entry: App.tsx), Zustand store, MUI (graphify: 140 nodes)
docs/          — формулы, БД, архитектура, деплой (см. таблицу в Блоке 2)
.claude/       — агенты, skills, команды Claude Code
```

**Команды:**
| Команда | Когда |
|---------|-------|
| `docker compose up -d` | Локальный запуск → http://localhost:3000 |
| `docker compose -f docker-compose.prod.yml up -d --build` | Прод (полный процесс → docs/DEPLOY.md) |
| `cd frontend; npm run dev` | Frontend standalone без Docker (Vite) |
| `cd frontend; npm run build` | Production build (tsc + vite build) |
| `docker compose exec backend alembic upgrade head` | Применить миграции (локально) |

**API Rate Limit:** 400 запросов/мин (verified experimentally 2026-06-07, NOT документация 100 токенов!)
- /lots = 2, /history = 2, /emission = 1 (Request-based, не token-based)
- Текущее: 54.5 запросов/мин (13.6% от лимита) — БЕЗОПАСНО
- Reserve: 345.5 запросов/мин (86.4%)
- **При оптимизации:** спрашивай разрешение перед изменением LOTS_PER_RUN / BATCH_SIZE / REFRESH_INTERVAL
- Token Bucket → Redis (Lua). Fallback: in-memory.

Детали → читай docs/

---

## Блок 5: Специализированные агенты

Для объёмных задач — делегировать subagent'у (Agent tool):

| Агент | Когда вызывать |
|-------|---------------|
| `researcher` | ПЕРВЫМ для любой нетривиальной задачи — исследует, планирует, пишет ТЗ |
| `backend-dev` | Реализация: FastAPI, SQLAlchemy, Celery, Alembic |
| `frontend-dev` | Реализация: React, MUI, Zustand |
| `designer` | Макеты, прототипы в design/, дизайн-спека |
| `tech-writer` | После задачи: обновление docs/, CHANGELOG.md, CLAUDE.md |
| `qa-tester` | Предлагать после реализации — анализ багов, проверка API/логики |
| `deploy` | Предлагать после тестирования — деплой на VPS |
| `security` | Перед деплоем или по запросу — OWASP ревью |

Агенты следуют правилам коммуникации из Блока 3 (план → подтверждение → выполнение).

**Skills** (вызываются автоматически по контексту задачи): `test-driven-development` (перед написанием кода), `systematic-debugging` (при отладке багов), `using-git-worktrees`, `verification-before-completion`, `receiving-code-review`, `finishing-a-development-branch`, `dispatching-parallel-agents`, `writing-skills`, `claude-md-improver` (аудит этого файла, команда `/revise-claude-md`).

---

## Блок 6: Karpathy Coding Skills

Четыре принципа качественного кодинга (применять при любой задаче):

**1. Think Before Coding — думай перед кодом**  
Не предполагай. Не прячь сомнения. Выноси трейдоффы на поверхность.  
Если запрос допускает несколько трактовок — перечисли их и уточни.  
Задавай вопросы до реализации, а не исправляй после.

**2. Simplicity First — простота прежде всего**  
Минимальный код, который решает именно эту задачу — не больше.  
Никаких спекулятивных фич, лишних абстракций, ненужной конфигурируемости.  
Проверка: «Скажет ли senior-инженер, что это overcomplicated?»

**3. Surgical Changes — точечные изменения**  
Трогай только то, что необходимо для выполнения запроса.  
Соответствуй окружающему стилю кода.  
Каждая изменённая строка должна напрямую следовать из запроса пользователя.

**4. Goal-Driven Execution — исполнение от цели**  
Перед реализацией — сформулируй верифицируемые критерии успеха.  
Не «сделай так, чтобы работало», а конкретные чекбоксы для каждого шага.  
Реализация считается завершённой, когда критерии проверяемо выполнены.
