# Заметки и идеи

---

## Задачи в очереди

### Backend
- [ ] **Исправить Redis rate limiter в Celery** — воркер создаёт новый `asyncio.new_event_loop()` на каждую задачу, а `TokenBucketRateLimiter` — глобальный синглтон, кеширующий Redis-соединение. Соединение становится невалидным в новом loop → Lua-скрипт падает → автоматический fallback на in-memory. Работает, но только при 1 воркере. Фикс: не кешировать соединение между задачами (переинициализировать `_redis` при смене loop или создавать соединение внутри каждого вызова `acquire`).
- [ ] Telegram бот — уведомления о выгодных покупках
- [ ] Система апрувов — `approved_until` в `users`, проверка при логине, лимит Избранного
- [ ] Admin-панель — управление пользователями и апрувами

## Закрытые задачи

### Мониторинг — время обновления в карточке
- [x] Добавлена метка `last_successful_check` рядом с кнопкой удаления (правый верхний угол карточки)
- [x] Формат: если обновление сегодня — только время (`14:32`), иначе дата + время (`01.06 14:32`)
- [x] Реализовано через `formatLastCheck()` в `MonitoringPage.tsx`

---

### Мониторинг — онлайн-обновление
- [x] **Баг:** новый товар в watchlist не получал данные без перезагрузки страницы
  - `MonitoringPage` не имел polling-цикла — данные загружались один раз при монтировании
  - `ItemCard` не загружал лоты пока `stats === null` (новый товар без MarketStatistics)
  - Статистика для нового товара ждала планировщика (до 55 минут)
- [x] **Фикс фронтенд:** добавлен `setInterval(loadAll, 5 min)` — страница реально обновляется каждые 5 минут
- [x] **Фикс фронтенд:** лоты загружаются независимо от наличия `stats`, `PriceChart` отображается сразу
- [x] **Фикс backend:** добавлены задачи `collect_history_single` и `calculate_stats_single`; при добавлении товара в watchlist запускается цепочка лоты→история→статистика (~30–60 сек вместо часа)

---

## Система апрувов (доступ к сайту)

**Концепция:** закрытый портал — без апрува войти нельзя.

**Логика:**
- При регистрации статус: `pending` (ждёт апрув)
- Администратор выдаёт апрув на N дней
- Можно назначать лимит Избранного (сколько товаров можно добавить)
- После истечения — доступ закрывается, нужен новый апрув
- Без апрува: авторизация не проходит

**Backend:** поле `approved_until` (DateTime) в `users` + проверка при логине.

**Admin-панель:**
- Поиск пользователей (по имени / email)
- Список: статус (pending / active / expired), дата истечения
- Кнопка "Выдать апрув" → количество дней → подтвердить

---

## Идеи на будущее

- Уведомления в реальном времени — WebSocket push
- Сравнение регионов — один товар, несколько регионов рядом
