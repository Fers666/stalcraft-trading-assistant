# Заметки и идеи

---

## Задачи в очереди

- [ ] **Ревизия алгоритмов выгодности ← 2026-08-14** — разбор на прод-данных:
  `docs/tasks/profit-algo-review.md` (замеры, диагноз, 10 доработок P0–P2, внешняя практика).
  Ключевые находки: тир «Быстро» (`ref × 0.97`) не был связан ни с какой измеренной
  вероятностью исполнения — настоящий быстрый уровень `ref × 0.937`, ошибка 3 п.п.
  (числа первой редакции — `0.889` и «разрыв 8.3 п.п.» — **отменены**: они получены
  усреднением по сделкам, где выдачу перевешивают несколько ликвидных вариантов; корректная
  постановка — усреднение по вариантам, см. Шаг 2 ниже);
  **65 %** обещанной прибыли ленты стоит на 30 лотах с выборкой < 5 сделок; топ-10 ленты висит
  непроданным в среднем **972 мин** при глубине опоры 3 сделки и ликвидности 0.43 сд/день;
  у **65 из 109** предметов минимальный аск уже ниже `ref × 0.97`; покрытие `lot_start` **25.5 %**
  и ноль наблюдений дольше 48 ч (цензурирование); `signal_outcomes` показывает 99.5 % «продано»,
  потому что матчит любую сделку в полосе ±15 %, а не конкретный лот. Бюджет API ни один пункт
  не трогает. Порядок внедрения — §3 разбора.
  - [x] **Шаг 1 — P0-2: пол по данным и trim выбросов** — ТЗ
    `docs/tasks/ref-quality-floor.md` (актуальны §5–§7; §2.2 и §2.3 первой редакции
    **отменены Ревизией 1**). Эффективное число сделок (`Σ 0.5 ** (age_h / 48)`) вместо
    сырого count, отсечка выбросов по кратности, трёхуровневая `confidence`, гейт
    `below_floor` в ленте и watchlist. **Код реализован 2026-08-16** (`pricing.py`,
    `variant_stats.py`, `profitable_lots.py`, `market_stats.py`, `monitoring.py`,
    фронт `useLotStats.ts` / `LotStatCard.tsx` / `MobileLotStatCard.tsx`; 202 теста зелёные).
    **Ревизия 1 (§7 ТЗ) внедрена там же по итогам QA:** усадка к родителю удалена целиком
    (спасала 3665 из 3665 вариантов, 66.3 % опор лежали вне диапазона реальных сделок),
    MAD-trim заменён отсечкой по кратности `TRIM_RATIO = 3.0` (MAD схлопывался на круглых
    ценах: `wgwz` — полоса 0.4 % от цены, срезано 19 сделок из 39), подписи уверенности
    на фронте переформулированы, чтобы не спорить с показанным числом сделок (§7.3).
    Замер на стенде после пересчёта: `feed_lots` 682 → **332** строки, доля прибыли на
    выборках < 5 сделок 55.8 % → **0 %**, вариантов ниже пола 0 → **1738**, вариантов
    `shrunk_history` 1945 → **0**, опор вне диапазона сделок **0 из 763**.
    **Выкачено на прод 2026-08-16, миграция `0039` применена** (схема прода — `0041`).
    Правило деплоя, подтверждённое этим шагом: `alembic upgrade head` обязателен **ДО**
    запуска нового образа — модель объявляет колонку, поэтому без неё падает любой
    ORM-SELECT `MarketStatistics` (карточка предмета, watchlist-задачи,
    `calculate_market_stats`; на стенде — 94 `UndefinedColumnError` за 10 минут);
  - [x] **Шаг 2 — P0-1: калибровка ценовых тиров по вероятности исполнения** — ТЗ
    `docs/tasks/quantile-sell-tiers.md`. **Реализован целиком 2026-08-16** (бэкенд +
    фронт): тиры `0.97 / 1.00 / 1.05` → откалиброванные `0.94 / 1.00 / 1.06`
    (`FAST_RATIO` / `NORMAL_RATIO` / `PREMIUM_RATIO`), в каждый элемент `sell_options`
    добавлено `fill_probability` (75 / 50 / 25) — доля сделок варианта, проходящих по
    цене тира или выше; новая `pricing.tier_prices(ref)` — единственное место применения
    множителей, копия в `market_stats.py` удалена (теперь импорт); фронт показывает эту
    долю в блоке «Варианты продажи» с тултипом (подпись — «сделок дороже», см. поправку
    ниже). Формула прибыли, `evaluate_lot_profit`, пороги
    маржи и сортировка **не менялись**, тир расчёта прибыли остаётся `fast`. Калибровка —
    6 срезов прод-данных за три недели (`q25 = 0.937`, `q50 = 0.997`, `q75 = 1.058`,
    разброс ±2 %). **Методологическая поправка:** числа первой редакции разбора
    (`q25 = 0.889`, «разрыв 8.3 п.п.») получены усреднением **по сделкам** — оборот
    сосредоточен в нескольких ликвидных вариантах и перевешивает выдачу; тир применяется
    к варианту независимо от оборота, поэтому усреднять надо **по вариантам**, и реальная
    ошибка старой константы — **3 п.п.**, не 8.3. Замер на стенде: `feed_lots`
    330 → **228** строк (−30.9 %), контрфактический сдвиг прибыли по строке **3.43 п.п.**
    (11.31 % → 7.88 %), все **96** строк нынешней полосы 0–3 % раньше имели ≥ 3 %
    (прежний слой 0–3 % отсеялся целиком), доля прибыли на выборках < 5 сделок осталась
    **0 %** (регресс P0-2 держится); 208 тестов зелёные, сборка фронта EXIT=0. Миграций
    нет, бюджет API не затронут. **Выкачено на прод 2026-08-16** (вместе с P0-2 и
    миграцией `0039`).
    **Поправка 2026-08-16 к смыслу `fill_probability`:** это НЕ вероятность продажи лота,
    а ценовая позиция относительно потока **состоявшихся** сделок — величина посчитана по
    их ценам и ничего не знает о лотах, которые не продались (по `lot_observations` таких
    ~27 %). Константы `0.94 / 1.00 / 1.06` верны, неверна была интерпретация. UI
    переформулирован: «сделок дороже» вместо «шанс продажи» (коммит `0f8c3ec`);
    **Урок (повторная ошибка того же класса):** два критерия приёмки в ТЗ были
    сформулированы неверно — «средний `profit_pct` упадёт на 3 п.п.» и «полоса 0–3 %
    опустеет». Оба меряют агрегат по популяции, которую сам фильтр и отбирает, поэтому
    эффект в них не виден. Честная метрика — **контрфакт по одним и тем же строкам**.
    Помнить при написании критериев для следующих шагов;
  - [x] **Шаг 3 — P1-4: срок продажи с цензурированием ← 2026-08-17.** **Фазы A и B завершены и
    выкачена на прод 2026-08-16** — ТЗ `docs/tasks/lot-observations.md`, коммиты `61e7e8e`
    (таблица `lot_observations`, миграция `0040`, резолвер) и `b29ab85` (расход сделок +
    состояние стакана, миграция `0041`). Работает: наблюдение за **всеми** лотами свипа
    ленты, три исхода, `matched_sale_id` (одна сделка = максимум одно наблюдение),
    `queue_rank` / `cheaper_units` / `variant_live_lots` по варианту. Прод-распределение
    исходов — `sold` 73.2 % / `withdrawn` 16.3 % / `expired` 10.4 %, то есть **~27 % лотов
    не продались** (раньше эта масса была невидима). Детали — `docs/BUSINESS_LOGIC.md` §19.
    **Фаза B выполнена и выкачена 2026-08-17** — ТЗ `docs/tasks/sale-survival-curve.md`,
    таблица `sale_survival`, миграция `0043`, задача `recalc_sale_survival` (раз в сутки).
    Ждать 2026-08-30 не пришлось: восстановление истории дало объём раньше срока.
    - **Заменены двенадцать никогда не измерявшихся чисел** — множители `0.4/1.0/2.5` и
      лестница `2/8/24 … 72/168/336` часов в `make_sell_options`. Замер: реальные медианы
      **0.79 / 0.99 / 1.01 ч** против обещанных лестницей **24 / 72 / 168** для той же
      ликвидности. Ошибка была в порядке величины, а не в процентах;
    - **Два признака, сопоставимых по силе.** Ранговая корреляция с «продан ≤ 6 ч»:
      позиция в стакане **−0.470**, цена к опоре **−0.444**, взаимная 0.582. Лента
      считает по позиции не потому, что признак сильнее, а потому что стакан у сборщика
      в руках и позиция есть даже у вариантов без опоры; в `sell_options` стакана нет.
      Сырую глубину (`cheaper_units`) использовать нельзя: немонотонна, смешана с
      ликвидностью;
    - **Измеренная кривая:** `P(продан ≤ 6 ч)` от **73.7 %** в верхних 10 % книги до
      **10.1 %** в нижней половине; доля не продающихся вовсе — от 20 % до **86 %**.
      Этого разброса система не видела вообще;
    - **Три находки в уже работающем коде** (все — из замера, не из ревью):
      1. **Длительность аукциона переменная**, а не 48 ч: 48 ч — 47.0 %, **12 ч — 38.9 %**,
         24 ч — 11.1 %, 6 ч — 3.0 %. 42 % лотов физически не доживают до горизонта 24 ч,
         поэтому вероятность обязана считаться с административным цензурированием.
         Допущение «лот живёт 48 ч» было записано в `CLAUDE.md`, `models.py` и ТЗ фазы A —
         поправлено в `docs/DATABASE.md` и `docs/BUSINESS_LOGIC.md` §20;
      2. **Слепой зоны в начале жизни лота нет** — тревога не подтвердилась. Лот нельзя
         купить первые ~10 минут после `start_time`: по независимому источнику
         (`sales_history.lot_start`, 108 196 сделок) доля сделок с возрастом лота ≤ 10 мин
         равна **0.0 %**. Задержка входа сборщика (мода 10 мин) совпадает с моментом, когда
         лот становится доступен. Инцидентная когорта чистая, поправка не нужна;
      3. **Подпись `fill_probability` не описывает ничего измеримого** — 75/50/25 против
         измеренных **72 / 71 / 56 %**. Плоский участок кривой лежит между «Быстро» и
         «Нормально» (1.1 п.п. за 6 % цены — тир «Быстро» почти бессмыслен), а «Выгодно»
         стоит **15.7 п.п.** за те же +6 %. Дальше обрыв: `×1.10` → 44 %, `×1.30` → 23 %.
         Множители не меняются: по ценам сделок они верны;
    - **Восстановленные наблюдения в кривую НЕ идут** — и прежняя сверка требует поправки.
      Согласие источников 95.2 % измерено на общих лотах, а общее подмножество по
      построению состоит из долгожителей: лоты, которых видел только живой сборщик,
      продаются в **80.1 %** случаев с медианой жизни **0.61 ч**, а общие — в 56.9 % и
      4.11 ч. Плюс снапшот не видит глубоких стаканов (p90 книги **31** против **409**)
      из-за обрезки 200 лотами. Итог: на одних и тех же предметах снапшот даёт вероятность
      продажи ниже живого на 8–16 п.п. Позицию в книге он при этом измеряет верно
      (`corr = 0.992`) — смещена популяция, не измерение;
    - **Прод после деплоя:** первый прогон дал 59 строк (24 `pos`, 35 `ratio`), таблица
      сошлась с замерами в пределах 1 п.п., инварианты чисты (инверсий границ 0,
      немонотонностей 0). Покрытие после полного круга свипа — **193 из 193** строк ленты
      и **966 из 966** вариантов с реальными ценами продажи;
    - **Находка, которой не было в плане (обоснование шага 4).** Разрез ленты по новой
      вероятности: **119 из 154 строк (77.3 %)** имеют `P(продан ≤ 6 ч) < 40 %` и
      продаются вообще лишь в **12.8 %** случаев — и именно у них средняя прибыль
      **11.2 %** против 6.6 % у ликвидной полосы. **Самая высокая обещанная прибыль
      сосредоточена на лотах, которые почти никогда не продаются.** Механизм структурный:
      лот выглядит выгодным, когда его цена сильно ниже опоры, а продавать надо близко к
      опоре — то есть глубоко в стакане. Это прямое обоснование P0-3;
    - **🔴 Главная ошибка расчёта, найденная уже после выкатки (§7.5 ТЗ, коммит `b18af85`).**
      Кривая считалась **по выжившим**: в знаменатель шли только закрытые наблюдения, а
      лот закрывается через 2 ч после последнего появления — среди недавних успевают
      закрыться в основном быстро проданные. Лоты, всё ещё висящие на рынке непроданными,
      выпадали целиком, хотя лот, переживший горизонт на наших глазах, — это и есть
      наблюдение «за H не продался». Завышение **12.5–15.7 п.п.** по горизонтам и до
      **21.4 п.п.** по стратам, причём неравномерно (по медленным втрое сильнее), поэтому
      исказился и уровень, и **форма** кривой — отсюда отозванный вывод про «плоский
      участок». Отброшено было от четверти до половины наблюдений, и именно тех, что
      говорят «не продался». **Эта ошибка встретилась в задаче четырежды**
      (`fill_probability`, сверка источников на общих лотах, критерии приёмки P0-1,
      знаменатель кривой) — проверять рефлекторно: кто выпал из выборки и не выпал ли он
      именно потому, что с ним случилось то, что мы измеряем;
    - **Критерий приёмки №2 не выполнен и был сформулирован неверно:** требовалось
      изменение `est_sell_hours` у ≥ 60 % строк, факт — 41.5 %. Причина не в реализации:
      медиана страты `bottom` (1.99 ч) случайно совпала со старым эвристическим 2.00 ч.
      Доля изменившихся чисел меряет совпадение значений, а не смысл работы; честные
      критерии — «величина перестала быть выдуманной» и «появилась величина, которой не
      было», оба выполнены на 100 % строк. **Третий раз подряд критерий приёмки в ТЗ
      оказался хуже самой работы** — при написании следующих ТЗ формулировать критерий
      от того, что должно стать известно, а не от того, какие числа должны сдвинуться;
    - **QA после выкатки нашёл 5 дефектов, все починены (2026-08-17, §7.4 ТЗ).** Главный:
      `format_hours` схлопывала всё < 2 ч в «< 2 ч», и поскольку ВСЕ измеренные медианы
      лежат в 0.46–1.99 ч, строка «срок» стала одинаковой у трёх тиров — результат работы
      до пользователя не дошёл вообще (2916 из 2916 measured-опций). Остальные:
      `pct_sold_ever` считался на другом знаменателе, чем `p_sold_*` (выходил МЕНЬШЕ
      `p_sold_24h` во всех 59 стратах, в тултипе получалось 102.9 %); baseline-вызов
      логирования `signal_outcomes` не получал таблицу и писал старый эвристический
      прогноз; страта тира бралась по УСЕЧЁННОЙ цене (`int(ref*0.94)` при ref, не кратном
      50, даёт 0.9399992 — 30 % вариантов уезжали в соседний бакет, всегда в оптимистичную
      сторону); строгое `!== null` на мобильной ленте пропускало `undefined`.
      **Почему не поймали тесты:** `test_each_tier_lands_in_its_own_bucket` брал
      `ref = 100 000`, делящийся на 50, — проверка только удобного входа. Про отображение
      и про инварианты между полями тестов не было вовсе. Оба класса добавлены;
    - **Что осталось за рамками:** размер пачки (сигнал есть — стеки продаются быстрее
      одиночек, но n на два порядка меньше и смешан с типом товара, замер повторить через
      две недели), час суток по хазарду, детекция перевыставления (разложила бы `withdrawn`
      на две причины), дискретная модель хазарда — строить только если побьёт таблицу;
    - [x] **Восстановление истории лотов из снапшотов ← 2026-08-16** — ТЗ
      `docs/tasks/history-lot-reconstruction.md`, скрипт
      `backend/app/scripts/reconstruct_lot_history.py`, миграция `0042` (колонка
      `source`), коммит `4375d12`. **Реализовано, выкачено, сверено и прогнано на проде.**
      - **Сверка §5.2 пройдена** (`--verify`, read-only): `sold` 58.5 % против 61.2 %
        у живого (−2.7 п.п.), `expired` 21.9 / 21.1, `withdrawn` 19.6 / 17.7, медиана
        жизни проданных ×1.11. Оба источника увидели 12 467 общих лотов. Вердикт —
        согласуется;
      - **Прогон `--days 7` на проде:** записано **78 485** наблюдений, закрыто 72 206
        (`sold` 57.3 %, `expired` 20.5 %, `withdrawn` 22.3 %). 9 хронически обрезанных
        предметов исключены целиком (до 94 % срезов), из взятых пар обрезано 0.6 %
        снапшотов. Возвратов лота после ложного исчезновения — 0;
      - **Остановились на 7 днях сознательно:** недельный цикл покрыт, 72 тыс. закрытых
        наблюдений дают ~2 400 на страту при необходимых ~400, а более старые данные
        описывают уже сдвинувшийся рынок. Окно расширяется одной командой, если
        фазе B понадобится;
      - **Что обязана учесть фаза B:** левоусечённых **15.4 %** (на суточном окне было
        42.9 %) — брать инцидентную когорту; шаг обхода медиана **226 с**, p90 316 с,
        максимум 645 мин, поэтому горизонты только 6 / 24 / 48 ч, «продастся за час»
        на этих данных не строится; `withdrawn` — смесь «снял продавец» и «матч не
        нашёл сделку», считать две границы; `ref_price_at_seen` у восстановленных
        строк `NULL` по построению, опору фаза B считает сама по `sales_history`;
      - Состояние таблицы на проде: `live` 35 585 строк (закрыто 21 247),
        `snapshot` 78 485 (закрыто 72 206), 53 МБ;
  - [x] **Шаг 4 — P0-3: ранжирование по ожидаемой прибыли ← 2026-08-17** — ТЗ
    `docs/tasks/ev-ranking.md`, колонка `feed_lots.ev_per_hour`, миграция `0044`,
    коммит `443649c`. **Выкачено и принято на проде.**
    - `ev_per_hour = profit_total / est_sell_hours × P(продан ≤ 6 ч)` — один множитель
      к уже существующей колонке, ставший умолчанием сортировки;
    - **формулу выбрал замер, а не план.** Исходный разбор предлагал нормировать на
      капитал (`EV / (капитал × T)`) — отклонено: доходность корректна математически, но
      превращает ленту в список копеечных сделок (средняя прибыль топ-20 падает в 20 раз,
      капитал в 185, топ-10 состоит из лотов с прибылью 572 ₽). Капитал у пользователя —
      не связывающее ограничение. Из пяти замеренных кандидатов победил `p × profit / час`;
    - **существующая колонка «₽/час» проблему не решала**: её порядок совпадал с
      `profit_total` на **0.961**, потому что срок меняется втрое, а прибыль — на порядки;
    - результат на проде: топ-10 по вероятности продажи **24.6 → 35.8 %** при падении
      средней прибыли на **2.0 %**; топ-50 — 22.0 → 30.3 % при −3.0 %;
    - **критерий приёмки №1 снова оказался хуже работы** (требовалось ≥ 15 п.п., факт
      11.2): порог был снят с одного среза живого рынка за несколько часов до выкатки.
      **Четвёртый раз подряд.** Правило на будущее: числовой порог с одного среза рынка
      критерием быть не может — годится направление с оценкой цены либо порог, выведенный
      из свойств данных;
  - [x] ~~Шаг 5 — P1-5 (стена андеркатов) и P1-6 (проверка «почему это дёшево»)~~ —
    **оба отменены замерами 2026-08-16** (`docs/tasks/profit-algo-review.md`, врезки
    к P1-5/P1-6). P1-5: ограничивать цену выхода стаканом — двойной учёт конкуренции
    (91.1 % «конкурентов» — сами лоты из ленты, а тиры откалиброваны по состоявшимся
    сделкам, которые шли в присутствии конкурентов); сторона стакана поглощена фазой B
    как ковариата срока. P1-6: явления нет — чем дешевле лот, тем вероятнее он продаётся,
    лотов дешевле 35 % опоры не существует вовсе; теория требует **скрытого** качества,
    а `qlt`/`ptn` полностью наблюдаемы;
  - [ ] Шаг 6 — P1-7: честный fill rate в `signal_outcomes`, затем замкнуть калибровку;
  - [ ] Шаг 7 — P2-8…10: размер позиции, единая семантика риска, задействовать уже
    посчитанные сигналы (`best_sell_hour`, `bulk_spike`, тренды).
  - **Урок по итогам первых шагов (2026-08-16).** Из шести пунктов плана, до которых дошли
    руки, **три отменены замерами** — усадка опоры к родительскому уровню (P0-2, Ревизия 1),
    P1-5 и P1-6. Все три были гипотезами, привнесёнными из внешней теории, а не находками
    в данных; выживало ровно то, что выведено из замеров на прод-данных. Отсюда порядок
    работы для оставшихся шагов: **сначала замер на проде, потом ТЗ**, а не наоборот.
    Второе правило, повторившееся трижды: **любой агрегат по предмету смешивает разные
    товары — считать надо по вариантам `(item_id, qlt, ptn)`**, потому что `qlt`/`ptn` и
    есть главный ценообразующий признак (усреднение по сделкам вместо вариантов дало
    ложные «8.3 п.п.»; усадка через границу качества завысила опору до ×6.96; «стена
    андеркатов» померилась по предмету). Записано в `docs/tasks/profit-algo-review.md`
    §1.6 и `docs/BUSINESS_LOGIC.md` §19.
- [x] **Лента: расширение набора на снаряжение, части предметов, премиум и пропуска ←
  2026-08-17** — ТЗ `docs/tasks/feed-gear-expansion.md`. Набор ленты **103 → 382 предмета**
  (+279 торгуемых: оружие 111 из 249, обвесы 68 из 137, броня 33 из 75, части 55 из 123,
  премиум и пропуска 10 из 12, рюкзаки 2 из 10). Тестов **334 → 378**. Код реализован,
  на проде ещё не выкачен.
  - **Правило набора живёт в одном месте** — новый `backend/app/services/feed/scope.py`:
    группы `artefact` / `weapon` / `attachment` / `armor` / `backpacks` / `parts` / `pass`,
    строгий `on_auction IS TRUE` (было `IS NOT FALSE`), ранги veteran/master/legend **внутри**
    категорий снаряжения, шаблоны имени для частей, премиума и сезонных пропусков. Маска
    `artefact%` убрана из шести мест (сборщик ×2, `variant_stats`, `/feed/summary`, два
    скрипта). Одним предикатом по рангу набор не описывался: в категории `other` 361 предмет
    с высокими рангами — почти сплошь косметика, а сезонные пропуска пришли разовым импортом
    с `color = NULL`;
  - **Качество снаряжения — свойство предмета, а не лота.** Единый резолвер
    `pricing.resolve_quality()` / `resolve_variant_key()`: у не-артефакта в лоте нет
    `additional.qlt`, качество берётся из `master_items.color` (`_COLOR_TO_QLT`:
    `rank_veteran`→3, `rank_master`→4, `rank_legend`→5). Обе стороны ключа варианта
    (продажа в `variant_stats.variant_key`, лот в сборщике) обязаны считать одинаково —
    разойдись они, все продажи ветеранского ствола легли бы в вариант `(0, 0)`, сборщик искал
    бы `(3, 0)`, и лента по снаряжению вернула бы **ноль строк без единой ошибки в логах**;
  - **Кривая дожития получила измерение `class`** (`artefact` / `gear`): миграция `0046`,
    колонка `sale_survival.class`, уникальный ключ → `(class, feature, bucket, horizon_h)`,
    класс в сигнатурах `SurvivalTable.get()` / `summary()` и в `pricing.apply_survival()`.
    Классов ровно два — дробить мельче нельзя, страты не наберут `MIN_STRATUM_N = 200`.
    Gear-страт пока нет: потребитель деградирует так же, как артефакты до первого пересчёта,
    второй выкатки не требуется;
  - **API:** параметр `category` в `GET /feed/lots` — серверный фильтр по группам вместо
    клиентской эмуляции через `item_id[]`, которая на 382 предметах физически ломалась о кап
    `FEED_MAX_LIST_VALUES = 50`; счётчики групп в `GET /feed/filters`;
  - **Фронт:** таксономия групп в `api/feed.ts` (зеркало `scope.py`), чипы групп вынесены на
    видное место, новый общий `components/ui/ItemSearch.tsx` (автокомплит предмета — селект на
    382 позиции непригоден), корректная отрисовка неизмеренных величин (у снаряжения колонки
    прогноза пустые до накопления gear-страт);
  - **Замер подтвердил свойство рынка, а не дефект отбора:** чем выше ранг, тем реже предмет
    вообще торгуется (weapon veteran 49 %, master 38 %; armor master 19 %; attachment legend
    **0 из 5**) — высокотировая экипировка выменивается и апгрейдится, а не покупается.
    Согласуется с гипотезой A из `docs/tasks/audit-on-auction-status.md`;
  - **Что ещё не сделано по ТЗ:** миграция `0046` на проде не применена, разовый бэкфилл
    истории по новым предметам (`backfill_artifact_history.py`, ~8.1 млн записей против
    48.3 млн у артефактов) не запускался — без опор `ref_price` лента по снаряжению покажет
    ноль строк. Оба шага — только с явного подтверждения пользователя.
- [ ] **Отложенный дефект: `apply_survival` с чужим классом на двух путях ← 2026-08-17** —
  карточка «Избранного» (`GET /monitoring/item`) и `market_stats._calculate_sell_options`
  вызывают `apply_survival` с умолчанием `item_class = "artefact"`, потому что объекта
  `master` в этих путях нет вовсе. Сейчас безвредно: gear-страт в `sale_survival` ещё нет,
  `get()` возвращает `None`, потребитель деградирует. Но как только страты наберут
  `MIN_STRATUM_N = 200`, оба пути начнут показывать снаряжению **артефактные** вероятности и
  сроки — молча и правдоподобно. Лечение — протащить `MasterItem` (или готовый класс) до
  вызова; отдельная задача.
- [ ] **Переименовать «Ленту артефактов» в маркетинговом копирайте ← 2026-08-17** — набор
  больше не только артефакты, но тексты не тронуты (решение за пользователем):
  `frontend/src/pages/FaqPage.tsx` — два блока про «Ленту артефактов», включая фразу «она сама
  обходит все артефакты рынка»; `frontend/src/pages/LandingPage.tsx` — карточка фичи. Правка
  чисто текстовая, требует пересборки фронта.
- [ ] **Проверить непересечение групп ленты на проде ← 2026-08-17** — группа служит
  одновременно фильтром `/feed/lots?category=` и счётчиком чипа в `/feed/filters`, поэтому
  группы обязаны разбивать набор **без пересечений**: иначе подпись и выдача считаются по
  разным правилам. В коде это обеспечено порядком `FEED_GROUPS` (правила по имени идут раньше
  категорийных) и исключением шаблонов имени из категорийных правил; по замеру части и премиум
  лежат в категории `other` и с категорийными правилами не спорят. Убедиться на живых данных
  одним запросом: предмет, попавший больше чем в одну группу, — дефект набора.
- [ ] **Контрактные изменения ленты, которые надо помнить ← 2026-08-17** — не задача, а
  предупреждение потребителям API после расширения набора: `GET /feed/filters` →
  `categories[].value` теперь **ключ группы** (`weapon`, `parts`, `pass`, …), а не русская
  подпись (были «Био / Грав / Терм / Электро / Прочие»); `GET /feed/summary` → `items_tracked`
  сменил семантику с `on_auction IS NOT FALSE` на строгий `IS TRUE` — число упадёт, и это не
  регресс.
- [x] **Перегрузка воркеров: срок годности тиков + `concurrency` ← 2026-08-07** — коммит
  `a6829d6`, выкачено на прод. Диагноз: прогон `collect_all_active_lots` (22–38 с) длиннее
  такта 20 с, цикл ленты (58–73 с) — длиннее такта 60 с, а слотов было два; без `expires`
  очередь `collector` копилась бесконечно (**2432 задачи** на момент расчистки), отсюда ядро
  в полке и бесполезность апгрейда сервера — потолок задавал `worker_concurrency=2`, а не
  железо. Сделано: `options.expires` (20 с / 55 с / 110 с), `worker_concurrency` 2 → 3,
  разовый `celery purge -Q collector`. Замер после: очередь **0** (устойчиво 8 мин), load
  average 1.51 → 1.06, свежесть watchlist 140 с → ~110 с, лента снова собирает
  (`units=200`, `rows_upserted=557`), 429 нет. Подробности — `docs/SERVICES.md`,
  раздел «Срок годности периодических тиков».
- [ ] **Подтвердить расход API по счётчику лимитера ← 2026-08-07** — оценка по логам даёт
  **~340 из 400 ед/мин** (watchlist ~140 + лента 200), то есть 85 % лимита. Снято из логов,
  а не из `GET /admin/stats` — проверить на проде и, если факт близок к 400, решать по
  `FEED_BUDGET_UNITS_PER_MIN`. Цикл watchlist ~110 с вместо проектных 60 с принят как норма
  (решение пользователя): 60 с требуют 570 ед/мин и физически недостижимы при включённой ленте.
- [ ] **«Лента артефактов»: остаточные шаги перед выкаткой ← 2026-08-03** — фича
  реализована (ТЗ `docs/tasks/artifact-feed.md`, Фазы 1–6 + Ревизии 1–4;
  бэкенд `python -m pytest tests -q` → **172 passed**, фронт `tsc --noEmit` + `vite build`
  → EXIT=0), но **не проверена вживую после сужения объёма и не выкачена**:
  - [x] **объём сужен пользователем 2026-08-04** (ТЗ, §Ревизия 4): у ленты **нет
    уведомлений** и нет трансляции в полосу сигналов — раздел смотрят напрямую с портала.
    Удалены тип события `feed_lot` (продюсер + оба консьюмера + дедуп-ключи), ручка
    `GET /feed/signals`, параметр `lot_key` в `/feed/lots` с закреплённой строкой (вместе
    с ним — поверхность дефекта H1), три колонки `user_settings`, артефактные карточки
    полосы. Осталось четыре ручки: `/feed/lots`, `/feed/variant/{item_id}` (обе без
    гейта), `/feed/summary`, `/feed/filters` (обе за `feed_access`); смысл `feed_access`
    сужен до «полная лента». Трансляция «Избранного» в сигналы не тронута;
  - **браузерное QA после сужения не прогонялось** — критерии приёмки Фаз 2/4, §Р1.4 и
    §Р2.7 на живом приложении не подтверждены (часть из них к тому же отменена
    Ревизией 4);
  - [x] **блокер P1 закрыт**: составной `lot_identity_key`
    (`startTime|qlt|ptn|buyoutPrice|amount`) + `dedupe_rows` — цикл сбора падал целиком
    на `CardinalityViolationError`, потому что у предмета висят разные лоты с одинаковым
    `startTime`;
  - [x] `security` **прогнан 2026-08-04**, находки исправлены: H1 (`?lot=` в
    `/feed/lots` обходил лимит тарифа и персональный порог — сама поверхность дефекта
    исчезла: параметр `lot_key` и закреплённая строка удалены Ревизией 4), M3 (ошибка на
    одном предмете больше не роняет цикл сбора: `safe_scan_item` + счётчик отказов и
    парковка предмета), M2/L6 (серверная валидация порогов `0..100` + квантование порога
    витрины шагом 5), M4 (точечная выборка идёт по индексу, без `count(*)`), M5 (истёкший
    тариф не получает уведомления — `is_tier_expired`/`current_tier` в `get_tier_limits`;
    **побочно чинит `buy_alert` и `profitable_lot`** — фоновые консьюмеры читают
    пользователя мимо `get_current_user`), L9 (кап 50 значений в списочных фильтрах),
    L11 (`lot_key` → varchar(128)), L10 (предупреждение в `downgrade()` миграции `0038`);
  - [x] **дефекты повторного QA исправлены 2026-08-04** (ТЗ, §Ревизия 3): карточка ленты
    питается из `artifact_variant_stats` через новую ручку `GET /feed/variant/{item_id}`
    (было — 404 у 85 % предметов из-за `/monitoring/item`), цифры карточки и строки
    совпадают (батч-поправка + прогноз времени уровня варианта), сортировка «₽/час» идёт
    по новой колонке `feed_lots.profit_per_hour_total`, всплеск расхода API разведён
    (`crontab(minute="*")` + джиттер + `HISTORY_ITEM_DELAY`), парковка отказавшего
    предмета держится заданные 900 с;
  - ⚠ **на локальном стенде** дописанная в `0038` колонка отсутствует (миграция уже
    применена) — выполнить вручную, `downgrade` использовать НЕЛЬЗЯ (уничтожает
    глобальную `sales_history`):
    `docker compose exec postgres psql -U stalcraft -d stalcraft -c "ALTER TABLE feed_lots ADD COLUMN profit_per_hour_total numeric(14,2);"`;
  - миграция `0038` **на проде применена** (`alembic current` = `0038`, проверено прямым
    запросом 2026-08-16; локально тоже). Повторно накатывать нечего, а `downgrade` этой
    миграции применять **НЕЛЬЗЯ** — он уничтожает глобальную `sales_history`
    (строки с `user_id IS NULL`);
  - деплоя не было;
  - **beat-задачи ленты запустятся при первом же рестарте контейнеров** —
    `collect-artifact-lots` (`crontab(minute="*")`), `collect-artifact-history` (:15),
    `calculate-artifact-variant-stats` (:14,:24,:34,:44,:54). Расход внешнего API
    вырастет с 54.5 до ~158 запросов/мин (≈39.5 % лимита 400) — на проде включать
    только после явного подтверждения (см. CLAUDE.md, Блок 4);
  - **разовый бэкфилл истории 30 дней не запускался**
    (`docker compose exec backend python -m app.scripts.backfill_artifact_history --days 30`).
    Без него у вариантов нет `ref_price` → лента показывает **ноль строк**. Скрипт
    расходует лимит залпом — помнить инцидент 429 от 2026-06-29 (`BACKFILL_PAGE_DELAY=1.0`,
    `BACKFILL_MAX_PAGE_RETRIES=5` уже учтены).
- [ ] **Калибровка сборщика ленты после первого прогона ← 2026-08-03** — обязательный шаг
  §Калибровка ТЗ `docs/tasks/artifact-feed.md`. Длительность цикла спроектирована как
  **следствие** бюджета, а не входной параметр: предварительный замер `sum(lots_total)` по
  артефактам сделать не удалось (доступ к проду был заблокирован). Снять за первый час: `units` за
  цикл (лог `feed cycle #…`, норма ≤ 200), время полного круга (`feed sweep`, цель ≤ 120 с,
  приемлемо ≤ 240 с), `deferred`, `guard_trips` (норма 0), пиковый расход по
  `GET /admin/stats` (≤ 340), длину цикла «Избранного» (рост выше 90 с = лента душит
  основной сбор), долю вариантов с `ref_price` (> 0.5 после бэкфилла). Правила подкрутки —
  в ТЗ; **повышение `FEED_BUDGET_UNITS_PER_MIN` — только с подтверждения пользователя**.
  Итоговые значения констант записать сюда и в `docs/SERVICES.md`.
- [ ] **Прототип `design/v5/app/feed.html` разошёлся с реализацией ← 2026-08-04** (для
  `designer`, `tech-writer` файлы `design/v5` не трогает). После сужения объёма «Ленты
  артефактов» в прототипе остались состояния, которых больше нет в продукте:
  (а) **закреплённая строка** лота вверху таблицы (приходила по `?lot=` из уведомления);
  (б) состояние **«лот уже выкуплен»** для закреплённой строки, ушедшей из среза;
  (в) **артефактная карточка в полосе сигналов** — правила `.sig.art` / `.sig-kick` /
  `.sig-kick.art` в `design/v5/assets/base.css:671-675` (обводка + кикер, два признака
  ради A11Y-01/02); из-за второй строки-кикера полоса в прототипе выше фактической
  (в приложении `FEED_PANEL_H` вернулся к 54). Настроек ленты в `settings.html` нет —
  там чистить нечего. Всё перечисленное — следствие решения «лента — отдельный раздел,
  мониторинг с портала», а не дефект прототипа: приводить надо прототип к реализации,
  а не наоборот.
- [ ] **Баг: кнопка «Карточка» в «Радаре рынка» для неотслеживаемого предмета молча
  открывает чужой предмет** (найдено при реализации ленты 2026-08-03, вне её объёма) —
  `MonitoringPage.tsx:99-117` резолвит `navigate`-состояние только по собственному
  watchlist и при промахе падает в `sortedWatchlist[0]`. Естественное лекарство —
  `components/ArtifactModal.tsx` (уже есть, развязан от «Избранного»): открывать карточку
  модалкой прямо из Радара, а не навигацией на `/app/monitoring`.
- [x] **Опорная цена по свежести сделок вместо плоской медианы 7д ← 2026-07-29** — ТЗ
  `docs/tasks/reference-price-recency-weighted.md`, коммиты `9fa7ddf` (бэкенд + миграция
  `0037` + 25 тестов + доки) и `82ba8a2` (фронт, уехал внутрь мобильного коммита — файлы
  общие, разделить нельзя). Повод: карточка «Юла +15» предлагала лот за 5 950 000 ₽ как
  выгодный (БЫСТРО +316 200), хотя все сделки за 24ч шли в коридоре 6.0–6.6 млн — ниже
  медианы 7д (6.8 млн); безубыточность 6 263 158 приходилась на середину реального коридора.
  Корень: плоская медиана за 7 дней на трендовом рынке отражает цену ~3.5-дневной давности,
  а опорная цена считалась **в пяти местах по пяти разным формулам**; единственная с
  trend-guard срабатывала при падении >25% (наблюдалось −6%) и сравнивала с медианой
  **асков**, а не сделок. У ветки `/monitoring/item` с фильтром качества (карточка из
  «Избранного») защиты не было вовсе. Теперь `ref` = медиана продаж 7д, взвешенная по
  возрасту (`0.5 ** (age_h / 48)`), все пять мест сведены на один keyword-only
  `compute_reference`; тренд стал меткой, `ref` не двигает и публикуется только при
  ≥`MIN_REF_SAMPLES` сделок за 24ч. `median_price_7d` намеренно НЕ переопределена — остаётся
  описательной статистикой. Новые колонки `market_statistics.median_price_24h`/
  `reference_price`. UI: бейдж тренда, вторая линия «24ч», безубыток в строке лота, дефолт
  переключателя на «Неделе». Прогон `qa-tester` нашёл 6 дефектов — все закрыты (детали в
  `docs/CHANGELOG.md`). Эффект на опорном тесте: ref 6 600 000 вместо 6 850 000, прибыль
  **+131 900 вместо +362 275**.
- [ ] **Замерить сокращение числа сигналов после выкатки опорной цены** — локально измерить
  нельзя: в `signal_outcomes` нет границы «до/после», а уникальный индекс
  `(item_id, region, lot_start_time)` считает только новые лоты, поэтому счётчик по часам
  отражает аптайм стенда, а не строгость отбора. На проде снять до и после выкатки:
  `SELECT date_trunc('hour',created_at), count(*), avg(predicted_profit_pct) FROM
  signal_outcomes GROUP BY 1 ORDER BY 1 DESC LIMIT 48;`. Сигналов станет меньше — это цель,
  но Telegram-бот питается теми же сигналами, поток уведомлений просядет вместе с ними.
  Если провал в разы — крутить `REF_HALF_LIFE_HOURS` (сейчас 48ч).
- [ ] **Мелкие остатки после `qa-tester` по опорной цене** (не чинил сознательно, все
  низкой критичности): (а) `profitable_lots.compute_signals_for_entry` при пустой
  отфильтрованной истории ставит `vol_for_opts=None` ⇒ `sell_options=None` ⇒ список лотов
  всегда пуст — путь мёртвый по построению, но это осознанная консервативность (без сделок
  по данному качеству опираться не на что, а `ref` из аска дал бы циркулярность);
  (б) `useLotStats.ts` дублирует формулу безубытка в фоллбек-ветке по `/lots`, где
  бэкендового значения нет — устранимо только переносом расчёта в `/lots`; (в) в
  `SignalsResponse` нет `median_24h` (в ТЗ был, в реализации не понадобился — тултип
  бейджа живёт за счёт `stats.median_price_24h`); (г) `compute_reference` усекает
  `median_7d` через `int()` — для предметов дешевле 1 ₽ поле обнулится (минимальная
  медиана в базе 28 ₽, недостижимо).
- [x] **Точный статус торгуемости каталога `on_auction` (Фаза A + gear-митигация) ←
  2026-07-24** — ТЗ + расследование `docs/tasks/audit-on-auction-status.md`. Коммиты
  `e003f58` (Фаза A), `a9a98d6` (митигация weapon/armor), `b67ea84` (расширение gear-
  исключения на attachment/backpacks/weapon_modules). Миграция `0036` применена на проде,
  бэкфилл отработал (1445 TRUE / 879 FALSE, errors=0). Источник статуса «появляется ли на
  аукционе» переведён с эвристики `bind_state` (привязка ≠ торгуемость) на реальную проверку
  через Stalcraft API — новые поля `master_items.on_auction`/`auction_checked_at`/
  `history_total`/`lots_total` + индекс `ix_master_on_auction`. Разовая Celery-задача
  `audit_auction_status` (`backend/app/tasks/audit.py`) пробивает каждый `item_id` через
  `/history` (>0 → TRUE) → `/lots` (>0 → TRUE; оба 0 → FALSE); 404 → FALSE; транзиент → NULL.
  Resumable (`WHERE on_auction IS NULL`), покоммитно, self-throttle ~100 ед/мин, region RU.
  Запуск ТОЛЬКО вручную через `POST /admin/audit-auction-status` (beat НЕТ — после
  `refresh-catalog` дёргать вручную для новых предметов); прогресс — `GET` того же URL.
  Фильтр `/items` (Фаза A): `(on_auction IS NOT FALSE OR gear_exempt) AND (on_auction IS TRUE
  OR bind_state IS NULL OR bind_state NOT IN {PERSONAL_ON_GET,DROP_ON_GET})`. `bind_state`
  теперь лишь fallback для непроверенных (`NULL`). ~519 непродаваемых **не-gear** (квесты,
  валюта, чертежи, крафт-патроны, поношенные/арена-стволы) убраны; всё gear видимо
  сознательно (каталожный id части gear даёт 0/0, но отделить непродаваемое от торгуемого-
  под-другим-именем через API нельзя — не теряем живое оружие). id-mismatch как баг НЕ
  подтвердился. Детали — `docs/DATABASE.md`, `docs/SERVICES.md`, `docs/BUSINESS_LOGIC.md` §6.
  Гибридный фильтр — **финальный дизайн**, а не транзит: отдельная «Фаза B» (жёсткий
  `on_auction IS TRUE`) НЕ нужна — `bind_state`-fallback постоянно защищает новые непроверенные
  предметы (on_auction=NULL) из `refresh-catalog`. A/B по gear разрешён: пользователь подтвердил
  (AK-103 реально не торгуется), id-mismatch как баг отсутствует; gear держим видимым по решению.
  Опционально на будущее: периодический ре-чек аудита (сейчас ручной по выбору пользователя).
- [x] **Онбординг-подсказки в пустом «Избранном» ← 2026-07-24** — новые пользователи
  не понимали механику Избранного (первый экран после входа — пустой список) и не
  начинали пользоваться порталом. Решение (подтверждено пользователем): подсказки в
  пустом состоянии, НЕ авто-добавление при регистрации. Backend — новый обезличенный
  эндпоинт `GET /watchlist/suggestions` → `List[WatchlistSuggestion]` (`item_id`,
  `name_ru`, `name_en`, `icon_path`, `has_profitable`, `is_popular`): **cache-read-only**
  — читает готовый кэш агрегата Радара (`_read_cached_aggregate()`), на промахе отдаёт
  `[]` и НЕ запускает тяжёлый пересчёт (защита от DoS-вектора, security-ревью), дедуп по `item_id`
  (max по метрикам), исключает уже добавленные пользователем предметы и предметы без
  master-записи, ранжирует «сначала выгодные → потом популярные», отдаёт только
  булевы флаги без чисел watchers/profit (`is_popular = watchers ≥
  SUGGEST_POPULAR_MIN_WATCHERS`, порог 2). **НЕ гейтится аддоном Market Radar**
  (осознанно — обезличенная выжимка ниже платного Радара, чтобы не подрывать его
  ценность). Frontend — пустое состояние `MonitoringPage.tsx` переписано с «заголовок
  + кнопка Каталог» на онбординг: объяснитель механики (цикл «отслеживай → находим
  выгодные лоты → сигнал в ленту»), сетка карточек-подсказок с мягкими бейджами «Есть
  выгодные лоты» / «Популярное» (без цифр), добавление в один клик через `POST
  /watchlist/` (region 'RU'), скелетоны при загрузке, деградация до ссылки «Смотреть
  весь каталог» при пустом ответе. Детали — `docs/BUSINESS_LOGIC.md` §«Радар рынка»
  (подраздел «Подсказки пустого Избранного»), `docs/SERVICES.md` (market_radar.py).
  Файлы: `backend/app/services/analytics/market_radar.py`,
  `backend/app/api/v1/endpoints/watchlist.py`, `frontend/src/pages/MonitoringPage.tsx`.
- [x] **Fix: статистика карточки Избранного под фильтром qlt/ptn ← 2026-07-24** —
  ТЗ `docs/tasks/fix-favorite-card-stats-qlt-ptn.md` (buy-side = вариант B1), коммит
  `0367a96`. Ветка «с фильтром качества/заточки» эндпоинта `get_item_stats` раньше
  пересчитывала под фильтр только ценовые окна (median/volume/volatility/sell_options), а
  sell-side timing (`best_sell_*`, `sell_hours_by_day`, `weekend_bonus`), `avg_sell_time_hours`
  и `batch_stats` брала из агрегата `market_statistics` (по всему предмету). Теперь эти поля +
  buy-side (`best_buy_*`, `buy_hours_by_day`) считаются per-request из отфильтрованного
  `SalesHistory`. Новые чистые хелперы `derive_sell_timing(sales)` / `derive_buy_timing(sales)`
  в `market_stats.py` (`weighted_score` + `WEIGHT_PRICE`/`WEIGHT_VOLUME` подняты на уровень
  модуля); `monitoring.py` — 2 scalar-запроса заменены одним фетчем строк за 30д. buy-side под
  фильтром = прокси B1 (час/день с минимальной средней ценой отфильтрованных продаж) —
  осознанный семантический сдвиг источника ТОЛЬКО для ветки с фильтром; агрегатная ветка
  (`best_buy` из снэпшотов `CollectedData`) и поведение `calculate_market_stats` не менялись.
  Детали — `docs/BUSINESS_LOGIC.md` §«Статистика артефактов…», `docs/SERVICES.md`
  (market_stats.py). Файлы: `backend/app/services/analytics/market_stats.py`,
  `backend/app/api/v1/endpoints/monitoring.py`.
- [ ] **Рассинхрон агрегатных ценовых окон под фильтром qlt/ptn** (пред-существующее, вне
  фикса `0367a96`, зафиксировано 2026-07-24) — в ветке `/monitoring/item` с активным фильтром
  качества/заточки поля `min_price_7d`/`max_price_7d`/`avg_price_7d` и окна 24h/48h всё ещё
  берутся из агрегата `market_statistics` (по всему предмету), рассинхрон с отфильтрованной
  медианой. На карточке эти поля напрямую не отображаются → визуального дефекта нет; для
  полноты честного пересчёта под фильтр их тоже стоило бы считать из отфильтрованного
  `SalesHistory` за 30д.
  **Частично закрыто 2026-07-29** (`9fa7ddf`): `sales_volume_24h` в этой ветке теперь
  считается по отфильтрованной выборке — иначе UI гейтил линию «24ч» и бейдж тренда по
  объёму всего предмета и показывал их там, где сделок под фильтром почти нет. Остальные
  перечисленные поля по-прежнему из агрегата.
- [x] **Fix: добавление вариаций предмета в избранное (каталог) ← 2026-07-24** — коммит
  `00b0e7e`. Кнопка «в избранное» в строке каталога защёлкивалась по одному `item_id` после
  первого добавления и блокировала добавление того же предмета с другой заточкой/качеством/
  регионом (бэкенд различает записи по кортежу `item_id+region+qlt+ptn`). Теперь кнопка всегда
  кликабельна, галочка `BookmarkOk` — информативный индикатор «уже отслеживается», не блокирует
  повторное добавление вариации. Плюс перевод 409-detail «Already in watchlist» → «Уже в
  избранном». Файлы: `frontend/src/pages/CatalogPage.tsx`,
  `backend/app/api/v1/endpoints/watchlist.py`.
- [x] **Web Push уведомления через RabbitMQ ← 2026-07-20** — ТЗ
  `docs/tasks/web-push-notifications.md`, миграция `0035` применена локально,
  QA пройден. Второй канал доставки (браузерный push, ПК + Android + iOS)
  **параллельно** Telegram, с минимальной задержкой через настоящую очередь
  событий. Продюсер (коллектор) кладёт лёгкое `{type, user_id, item, ...}` в
  RabbitMQ exchange `push.events`; отдельный контейнер `push_service` слушает
  очередь, курирует (гейт `notify_browser_push` + тариф, дедуп `push_*_sent:*`,
  все устройства) и шлёт web push (pywebpush/VAPID). Три типа событий (лоты /
  Buy Sniper / выброс). Новое: таблица `push_subscriptions`, эндпоинты
  `/push/*`, `push_broker.py`, Service Worker + PWA-манифест + тумблер
  «Browser Push» (subscribe-flow, iOS-инструкция), сервисы `rabbitmq` +
  `push_service` в оба compose. Детали — `docs/CHANGELOG.md`, `docs/DATABASE.md`,
  `docs/SERVICES.md`, `docs/ARCHITECTURE.md`, `docs/DEPLOY.md`.
  **Задеплоено на прод 2026-07-20** (прод-VAPID + отдельный пользователь
  RabbitMQ из `.env`, не guest). Security-ревью пройдено, фиксы применены
  (коммит `e4e2949`): allowlist push-хостов в `/push/subscribe` (анти-SSRF) +
  cap 20 подписок, `timeout` в `webpush()`, dev-порты RabbitMQ на `127.0.0.1`.
  Приём push подтверждён вживую (реальные лот-пуши + тестовый emission
  `sent=2/2` дошли до устройства). У одного устройства push не показывался —
  причина на стороне браузера/ОС (разрешение/показ), не код.
- [x] **Перевести Telegram-уведомления на ту же очередь событий (RabbitMQ) ←
  2026-07-21** — ТЗ `docs/tasks/telegram-notifications.md`, QA + security-ревью
  пройдены, **не задеплоено** (коммит ещё не сделан). Рефакторинг доставки, не
  новая фича: `telegram_bot` стал консьюмером `push.events` (своя durable-очередь
  `telegram.notifications`, привязана к тому же DIRECT-exchange по routing_key
  `push`, `x-message-ttl=15 мин`) вместо polling-цикла `_notifier_loop` (чтение
  `signals:*`/`buymin:*`/`EmissionEvent` каждые 15с). Fan-out на стороне брокера:
  DIRECT-exchange отдаёт копию каждого события обеим очередям (`push.notifications`
  для web push + `telegram.notifications`). Продюсер (`collectors.py`,
  `push_broker.py`) и схема БД НЕ менялись (миграций нет). `_notifier_loop` заменён
  на `_consume_loop` с супервайзер-петлёй для самовосстановления. Дедуп выброса
  переведён с БД-флагов `EmissionEvent.notified`/`end_notified` на Redis-ключ
  `tg_emission_sent:{event_id}:{phase}` (флаги стали вестигиальными для Telegram,
  но продюсер их по-прежнему заполняет). Prod: `telegram_bot` ходит в RabbitMQ под
  `${RABBITMQ_USER}/${RABBITMQ_PASSWORD}`, `depends_on rabbitmq healthy`. Три типа
  уведомлений (лоты / Buy Sniper / выброс) и flow привязки `/link` без изменений.
  Побочно — durable-очередь смягчает потерю уведомлений при рестарт-лупе бота
  (проблема нестабильной сети прод→api.telegram.org). Детали — `docs/CHANGELOG.md`,
  `docs/SERVICES.md`, `docs/ARCHITECTURE.md`.
- [ ] **Web push: deep-link клика по уведомлению на конкретный предмет** — идея
  пользователя 2026-07-20 (после прод-деплоя push). Сейчас Service Worker при
  клике открывает захардкоженный раздел (`render_*` в `push_service/consumer.py`:
  лот → `/app/lots`, закупка → `/app/buy-sniper`, выброс → `/app`). Надо вести на
  страницу **того** предмета, по которому пришло уведомление. Данные в событии
  уже есть (`item_id`/`region`/`quality_filter`/`enchant_filter`). **Ключевой
  нюанс:** клик по push открывает URL (`clients.openWindow`), а не React-Router
  `location.state` — поэтому мало дописать `?item=` в `url`, целевая страница
  должна **читать предмет из query-параметра** (`useSearchParams`). Сейчас
  пред-выбор работает только через `location.state` (`MonitoringPage.tsx:89-105`,
  `LotsPage.tsx:192-203`) — расширить на `?item=`. Маршруты — `/app/monitoring`,
  `/app/lots`, `/app/buy-sniper` (`App.tsx:60-64`). Правки: `push_service/
  consumer.py` (`render_profitable_lot`/`render_buy_alert` — адресный `url`),
  фронт `MonitoringPage.tsx`/`BuySniperPage.tsx` (чтение `?item=`). **Открытый
  UX-вопрос (не решён пользователем):** выгодный-лот вести на карточку Избранного
  (`/app/monitoring?item=`, богатый анализ) ИЛИ на список «Лоты»
  (`/app/lots?item=`, требует тариф advanced_plus+). Требует пересборки фронта +
  редеплоя.
  **Пункт снова полностью открыт.** «Лента артефактов» (Фаза 5) закрывала его частично —
  событие `feed_lot` вело адресным `url` на `/app/feed?lot={lot_key}`, а `FeedPage` /
  `MobileFeedPage` читали `?lot=` через `useSearchParams` и закрепляли строку. Всё это
  **удалено 2026-08-04** вместе с уведомлениями ленты (сужение объёма). Готовый образец
  «адресный url + чтение query-параметра на целевой странице» остался только в истории
  git — паттерн рабочий, воспроизводим при возврате к теме.
- [x] **Раздел «Закупки // Buy Sniper» (замена «Склада») ← 2026-07-19** — ТЗ
  `docs/tasks/buy-sniper.md`, миграция 0034 применена, QA пройден. Порог цены на
  товар из «Избранного» → Telegram-алерт «пора покупать», когда самый дешёвый лот
  ≤ порога. Новая таблица `buy_alerts` (drop `user_inventory`/`sell_recommendations`),
  эндпоинты `/buy-sniper/*` (+ `price-window` min/median/max за 3д), два тарифных
  флага `buy_sniper_access` (advanced+) / `buy_sniper_notifications` (advanced_plus+),
  Redis `buymin:*` из коллектора + `notify_buy_alerts` в боте. Frontend
  `BuySniperPage.tsx`, пункт навбара «Закупки» с гейтом; старый «Склад» удалён.
  Детали — `docs/CHANGELOG.md`, `docs/DATABASE.md`, `docs/SERVICES.md`,
  `docs/BUSINESS_LOGIC.md` §17.
  **Отложено (не в этом объёме):** browser-push для того же триггера
  (`UserSettings.notify_browser_push` уже есть); «% ниже медианы» как альтернатива
  абсолютному порогу; `last_triggered_at` в UI (дедуп живёт в Redis).
- [x] **Внедрение Design v5 «Терминал» ← 2026-08-03** — по спеке
  `docs/tasks/design-v5-implementation.md` (7 фаз, независимый деплой каждой).
  **Фазы 1–6 внедрены** (← 2026-07-18: фундамент токенов + 17 ui-компонентов,
  шелл/навбар, Избранное-эталон, Каталог+Лоты, Радар+Склад+Настройки+Новости;
  ← 2026-07-19: Фаза 6 — Лендинг + Логин, публичные страницы; детали —
  `docs/CHANGELOG.md`, conformance Избранного —
  `docs/tasks/design-v5-favorites-conformance.md`).
  **Фаза 7 «Лента» закрыта 2026-08-03** — но не тем содержанием, что планировалось
  в спеке: вместо событийного потока spike/move (`/feed/events`, материализация
  событий) сделана фича **«Лента артефактов»** — таблица выгодных лотов по всем 103
  артефактам каталога, ТЗ `docs/tasks/artifact-feed.md`. Прототип
  `design/v5/app/feed.html` переписан с событийного на табличный, `FeedPage.tsx` /
  `MobileFeedPage.tsx` больше не заглушки, добавлен `components/ArtifactModal.tsx`.
  Приёмка Фазы 7 из спеки («4 типа сигналов») **не применима** — заменена критериями
  ТЗ ленты. ⚠ Раздел проверен только сборкой; QA/security/деплой/миграция `0038` на
  проде и калибровка — открытый пункт «Лента артефактов: остаточные шаги» выше.
  Риск фазы 1 (тема сменилась глобально) остаётся: страницы FAQ/Admin/Register вне
  охвата прототипа, конформность не проверена (News приведена к DEL-01 в Фазе 5).
  **Известные пробелы (отложены, требуют backend-surface):** медиана 7д в
  сайдбаре Избранного не выводится (нет поля в `/watchlist`); предмет `dm6l2`
  без иконки (нет исходника у EXBO — фолбэк-буква). ~~P&L Склада → «—»~~ —
  неактуально: раздел «Склад» удалён и заменён «Закупками // Buy Sniper»
  (2026-07-19, см. выше).
- [ ] **Нестабильная сеть прод→api.telegram.org: прокси или форс IPv4 для
  `telegram_bot`** ← выявлено 2026-07-08 при деплое emission-ревизии.
  После пересборки `telegram_bot` ~14 мин крутился в рестарт-лупе с
  `telegram.error.TimedOut` на инициализации (`get_me`), затем сам поднялся
  (polling, статус Up). Это та же первопричина, из-за которой изначально
  терялись уведомления о выбросе (worker падал на Timed out). Теперь ВСЯ
  Telegram-рассылка (лоты + выброс) идёт через `telegram_bot` — при его
  недоступности/рестарт-лупе не приходят никакие уведомления. Новая логика
  смягчает частично (флаг `notified`/`end_notified` ставится только после
  успешной отправки, ретрай каждые 15с), но при недоступности бота дольше
  отсечки свежести 15 мин событие о выбросе гасится безвозвратно.
  **Смягчено с 2026-07-21** (перевод Telegram на RabbitMQ): durable-очередь
  `telegram.notifications` копит события во время рестарт-лупа и доставляет их
  после реконнекта (в пределах `x-message-ttl=15 мин` + guard свежести) —
  потеря уведомлений при коротких падениях бота устранена; глубинная сетевая
  причина (прокси/форс-IPv4) всё ещё открыта.
  **Кандидат-решение:** настроить `HTTPS_PROXY` для `telegram_bot` (PTB
  поддерживает из коробки) ИЛИ форсировать IPv4-маршрут до Telegram
  (вероятно висит IPv6-маршрут). Диагностика не завершена — curl-проверки
  IPv4/IPv6 с сервера не выполнены. Контекст — `docs/DEPLOY.md` (раздел
  «Нюансы»), `docs/tasks/emission-notify-via-bot.md`.
- [x] **Fix: задержки Telegram-уведомлений** ← 2026-07-06 — убран блокирующий
  `sleep(60)` на 429 в `StalcraftClient._request()` (retry естественно через
  `due_pairs` на следующем тике), один shared `aioredis`-клиент на весь батч
  `collect_all_active_lots` (опциональный `redis_client` в
  `rate_limiter.acquire()`), per-entry try/except в
  `notify_profitable_lots()`, лог фактической длины цикла обновления пары
  (наблюдаемость для причины B, `SIGNALS_TTL=300`с). Диагноз и реализация —
  `docs/tasks/telegram-notification-bug.md`, `docs/CHANGELOG.md`.
  **Открытые вопросы (причины C/D — не баг, ожидаемое поведение):** уточнить
  тарифы и персональные настройки прибыли (`min_profit_margin_percent`/
  `exclude_less_than_amount`) двух пожаловавшихся пользователей — тариф `base`
  не получает Telegram-уведомления by design, разные пороги прибыли легитимно
  дают разный список выгодных лотов.
- [x] **CPU-спайки: следующие шаги после прод-замеров** ← 2026-07-07 —
  прод-замеры сняты (85 уникальных пар watchlist, `calculate_all_market_stats`
  залпом 193–236с каждый час, ~2.7с/пара, однопоточный CPU-bound), выбран и
  реализован вариант «размазывание + дифф-пересчёт»: новая порционная задача
  `calculate_market_stats_batch` (10 слотов в час :12–:57, слот пары —
  `crc32 % 10`, дифф-пропуск чистых пар по `sales_history.collected_at`,
  force-круг 04:12–04:57 МСК), миграция `0032` (индекс `ix_sales_collected_at`),
  цепочка `.delay()` из `collect_all_history` удалена,
  `calculate_all_market_stats` оставлена как ручной инструмент. Коммит
  `cf239e1`, ТЗ — `docs/tasks/market-stats-spread-diff-skip.md`, задеплоено
  и проверено на проде 2026-07-07. Бонусом — фикс фильтра времени продажи
  (коммит `9a41653`): лот живёт max 48ч, фильтр аномалий `0 < hours <= 48`
  вместо 14 дней. Этап 2 (rollups) — отдельный пункт ниже.
- [x] **Arsenal items verification — добавление 317 предметов в каталог ← 2026-07-31** —
  разовый импорт 317 предметов (251 торгуемых + 66 непродаваемых) из внешних источников
  (Lunar, stalzone.wiki, stalzone-monitor, EXBO global-ветка) через скрипт
  `backend/app/scripts/import_arsenal_items.py` + проверка статуса торгуемости через прямые
  запросы `/lots`/`/history` к Stalcraft API. Добавлено 40 локальных иконок в
  `frontend/public/arsenal-icons/` (формат webp). **На проде (2026-07-31):** master_items 2324 → 2640,
  316 вставлено (1 запись уже существовала на проде до импорта; локальные 2328→2645 отражают
  состояние dev-БД, не идентичное проду). Иконка `wjgm2.webp` отдаётся `200 image/webp`, предметы
  `dm6l2`/`wjgm2`/`q1m4` подтверждены в БД с ожидаемыми `on_auction`/`icon_path`.
  Backend-dev + frontend-dev + qa-tester прошли. Критичных багов не найдено. ТЗ и результаты —
  `docs/tasks/arsenal-items-verification.md`.
- [ ] **Инкрементальные почасовые агрегаты (rollups) для market_stats —
  этап 2, НЕ срочно** — если фоновая стоимость пересчёта продолжит расти с
  объёмом sales_history/watchlist. Идея пользователя: предрассчитанные
  почасовые «кубики» (count/sum/min/max/sum_sq — складываются между собой;
  медианы НЕ складываются — им нужен SQL-перцентиль или сырые данные),
  статистика собирается из кубиков, а не из сырых строк. Контекст — раздел
  «На будущее (вне scope, этап 2)» в
  `docs/tasks/market-stats-spread-diff-skip.md` и пункт 2 плана в
  `docs/tasks/cpu-spikes-recurring-2026-07-06.md`.
- [x] **Повысить покрытие qlt/ptn в sales_history** — реальная причина оказалась
  не ограничением API, а багом парсинга ← 2026-06-29. Stalcraft API `/history`
  уже возвращает `additional.qlt`/`ptn` напрямую в каждой записи продажи
  (подтверждено живым запросом к API), но `_collect_history_for_item()`
  (`backend/app/tasks/collectors.py`) никогда не читал `record.get("additional")`
  — поле молча отбрасывалось, хотя запрос уже шёл с `additional=true`. Старый
  комментарий в коде, утверждавший обратное, был неверным предположением, не
  фактом. Фикс: `record.get("additional")` теперь первичный, authoritative
  источник qlt/ptn; снэпшот-матчинг (`find_lot_info`) сохранён только для
  `lot_start` (время выставления лота, для `time_on_market`). Подробности и
  диагностика — `docs/tasks/sales-history-qlt-ptn-coverage.md`.
  Дополнительно: разовый CLI backfill-скрипт
  `backend/app/scripts/backfill_sales_qlt.py` (`docker compose exec backend
  python -m app.scripts.backfill_sales_qlt --days 30`) для существующих
  записей без qlt — печатает смету по rate limit перед запуском.
  **Инцидент 2026-06-29:** первый запуск на проде (`--days 30 --yes`) почти
  сразу начал стабильно падать в `429` на каждой странице `/history` и не
  продвигался — причина в самом скрипте, не в общем rate limiter'е: постраничные
  запросы внутри `_backfill_pair()` шли без пауз (~5 запросов/сек) и при первом
  же `429` ронялась вся пара `(item_id, region)` без retry. Пользователь
  остановил скрипт вручную. Фикс — только в `backfill_sales_qlt.py`
  (`BACKFILL_PAGE_DELAY=1.0`, `BACKFILL_MAX_PAGE_RETRIES=5`), разбор причины —
  `docs/tasks/backfill-rate-limit-burst-fix.md`. **Повторный запуск на проде
  после фикса выполнен (запущен 2026-06-29) — завершён успешно (подтверждено
  2026-07-02).**
- [x] **Инцидент: CPU-спайки на проде раз в час** — устранён 2026-06-29
  (`HISTORY_CONCURRENCY=6`, параллельная обработка `collect_all_history`),
  внеплановый фикс, не из бэклога. Подробности — `docs/CHANGELOG.md`,
  `docs/SERVICES.md`. **2026-07-06: спайки вернулись в изменённом виде**
  (плато ~10 мин + отложенный скачок через ~20 мин) — новое ТЗ
  `docs/tasks/cpu-spikes-recurring-2026-07-06.md`, частичный фикс задеплоен,
  следующие шаги — см. открытый пункт «CPU-спайки: следующие шаги» выше.
- [x] **Опробовать skill `systematic-debugging`** — применён 2026-06-28 на
  реальном баге (тариф `base` видел полную историю продаж за все окна вместо
  24ч) — root cause найден чтением кода за один проход (незащищённый
  `/monitoring/sales-chart`), фикс точечный, без лишних итераций. Эффект
  положительный, закрепляем как практику. `test-driven-development` в этом
  заходе не использовался (правки тестировались curl/tsc, не автотестами) —
  оценку оставляем открытой на следующий backend-таск с тестами.
- [ ] **Роадмап подписок — оставшиеся фазы** (Phase 0 "Тарифы" реализована,
  см. `docs/CHANGELOG.md` и `docs/BUSINESS_LOGIC.md` §17):
  - [x] Статистика в админке (rate-limit consumption, уникальные карточки по
    всем пользователям) ← 2026-06-28, см. `docs/CHANGELOG.md`,
    `docs/SERVICES.md` (`GET /admin/stats`, `get_consumption_stats()`).
    Дополнено 2026-06-28 — 4 пункта, потерянных при первом восстановлении ТЗ
    (зашедшие сегодня, активные за неделю, счётчик подключивших Telegram,
    колонка Telegram в таблице пользователей): см. `docs/CHANGELOG.md`,
    `docs/SERVICES.md`, `docs/tasks/admin-stats-gaps.md`.
  - [x] Раздел новостей ← 2026-07-02, см. `docs/tasks/news-section.md`, `docs/DATABASE.md`, `docs/SERVICES.md`: таблица `news`, 6 эндпоинтов `/api/v1/news/*`, страница `/app/news` с inline-редактором для admin, фиксированные теги (обновление/тарифы/техработы/важно).
  - [ ] Форма обращений → Telegram админам.
  - [x] "Радар рынка" (кросс-юзерная агрегация watchlist, отдельный аддон не
    по тарифам, гейтинг через `User.has_market_radar_addon`; топ группируется
    по `item_id, quality_filter, enchant_filter` — один предмет может занимать
    несколько строк; метрика `profitable_offers_count` — число выгодных лотов
    в снэпшоте, дедуплицированное по лотам, не по watcher'ам) ← 2026-06-28,
    см. `docs/CHANGELOG.md`, `docs/BUSINESS_LOGIC.md` §17, `docs/SERVICES.md`
    (`GET /market-radar/`, `get_market_radar_aggregate()`).
    - [x] Ревизия 2026-06-29 — сортировка по `profitable_offers_count` (убыв.,
      `None`→0) вместо `watchers_count`, полная серверная пагинация
      (`page`/`page_size`, default 20) вместо жёсткого SQL-лимита топ-20,
      safety-cap `MAX_BUCKETS=500` на число бакетов. См.
      `docs/tasks/market-radar-sort-pagination.md`, `docs/CHANGELOG.md`,
      `docs/BUSINESS_LOGIC.md` §17, `docs/SERVICES.md`.
  - [x] FAQ-онбординг + копирайт лендинга под переименование игры в STALZONE
    ← 2026-06-29, см. `docs/tasks/faq-onboarding-stalzone-rebrand.md`,
    `docs/CHANGELOG.md`:
    - (a) ребрендинг видимого пользователю копирайта Stalcraft X → STALZONE
      (лендинг, логин, Swagger, приветствие Telegram-бота); технические
      идентификаторы внешнего API (`stalcraft.net`, `StalcraftClient`,
      Redis-ключи, Celery app name) сознательно не тронуты.
    - (b) новая страница `/faq` (MVP, без welcome-модалки/guided tour) — 12
      вопросов в 5 группах, MUI Accordion, ссылки из навбара/лендинга/экрана
      регистрации.
    - (c) внеплановое дополнение по ходу задачи — чип тарифа рядом с ником в
      навбаре (`Layout.tsx`), `TIER_LABELS`/`TIER_COLORS` вынесены в общий
      `frontend/src/constants/tiers.ts` (рефактор дублирования с
      `AdminPage.tsx`).
  - [x] Ручной override лимита избранного (watchlist) вне тарифа ← 2026-06-28,
    см. `docs/CHANGELOG.md`, `docs/BUSINESS_LOGIC.md` §17 (подраздел «Override
    лимита избранного»), `docs/DATABASE.md` (`users.favorites_limit_override`).
  Черновой план остальных пунктов — в истории чата/архиве планов, при
  возврате к теме — заново зафиксировать через `researcher`.
- [ ] **Троттлинг рассылки выброса в Telegram** (техдолг, отложено 2026-07-21) —
  мягкий `sleep`/батчинг при fan-out события `emission` всем привязанным
  пользователям. Сейчас `handle_emission` шлёт всем подряд без пауз; при заметном
  росте числа привязанных возможен упор в Telegram flood limit (~30 msg/сек
  глобально). Вернуться ДО роста аудитории. Не затрагивает Stalcraft API rate
  limit (это внешний Telegram API). Контекст — `docs/tasks/telegram-notifications.md`
  (Security, п. 4).
- [ ] **`html.escape` текстовых полей в `build_lot_message`/`build_buy_message`**
  (техдолг, 2026-07-21) — сообщения шлются с `parse_mode=HTML`; названия предметов
  и прочие подставляемые строки приходят из внешнего API и не экранируются. Риск
  низкий (данные из каталога EXBO, не пользовательский ввод), но при появлении
  `<`/`&` в названии сообщение может сломаться/не отправиться. Экранировать
  подставляемые поля перед вставкой в HTML-шаблон.
  ~~**Образец готов (2026-08-03):** `build_feed_message`~~ — функция удалена 2026-08-04
  вместе с уведомлениями ленты; образец остался только в истории git. Приём тривиальный
  (`html.escape` над подставляемыми полями), делать по месту.
---

## API Аудит — Найденные пробелы (2026-07-02)

Полный аудит публичного API Stalcraft X (EXBO/Stalzone). Три агента исследовали кодовую базу + внешние источники (docs, GitHub, форумы). Детальный разбор — `docs/tasks/api-audit-2026-07-02.md` (создать при реализации).

### Незадействованные возможности API

- [x] **Emission-фича** ← 2026-07-06 — таблица `emission_events` (миграция `0031`), Celery-задача `collect_emission` (каждые 2 мин, 1 токен, Redis-дедупликация `emission:current_fingerprint`), эндпоинты `GET /api/v1/emission/current` и `/emission/history`, `EmissionWidget` в сайдбаре (красный при активном выбросе, золотой со счётчиком времени в ожидании, поллинг 15с/30с через `emissionStore`), Telegram broadcast всем `is_active AND is_approved AND telegram_chat_id IS NOT NULL` без гейтинга тарифом. Fix: `STALCRAFT_REGION` исправлен с `"EU"` на `"RU"`. Подробности → `docs/tasks/emission-tracker.md`. **Ревизия 2026-07-08:** рассылка перенесена из worker в `telegram_bot::notify_emission_events` (+ поле `end_notified`, миграция `0033`, + фильтр `UserSettings.notify_telegram`) — worker слал через одноразовый `Bot(token)` и терял отправки; см. `docs/tasks/emission-notify-via-bot.md`, `docs/CHANGELOG.md`. **Задеплоено на прод 2026-07-08** (`alembic current` = `0033` head, backend/worker/scheduler/telegram_bot пересобраны `--no-cache`). Операционный риск с сетью до Telegram — отдельный пункт ниже.
- [ ] **Пагинация лотов** — всегда `offset=0, limit=200`, дефолтная сортировка API неизвестна. Для предметов с >200 активными лотами `best_price_per_unit` может быть неточным. Исправить: добавить `sort=price&order=asc`, дозапросы offset=200/400... до < 200 лотов (max 1000). Учесть +2 токена за каждую доп. страницу.
- [ ] **Items Metadata (stalzone-database)** — в БД хранится только `item_id`. Полная база предметов с названиями (ru/en), категориями, цветами — на GitHub `EXBO-Studio/stalzone-database`. Перед реализацией: выяснить как сейчас приходят названия на фронт (пользователь сказал «приходят с бэка из какого-то источника» — источник не найден при аудите, требует проверки).
- [ ] **Пагинация истории** — `collect_all_history` берёт только offset=0, limit=200 раз в час. Для популярных предметов (>200 продаж/час) часть истории теряется навсегда. Аналогичное исправление через дозапросы.
- [ ] **Мультирегион** — API поддерживает RU/EU/NA/SEA, используем только RU. Кросс-региональный арбитраж недоступен. Большой scope, отложено.

### Баги и слабые места реализации

- [ ] **Snapshot-History Matching хрупкость** — окно поиска ~1.7 часа (200 снэпшотов × 20 сек). Медленно продающиеся предметы: `lot_start = NULL` навсегда. `avg_sell_time_hours` основан только на быстрых продажах — нерепрезентативен. Сложный фикс, после emission+пагинации.
- [ ] **dead column `detected_buyouts_count`** — всегда NULL в `CollectedData`. Удалить при следующей миграции.
- [ ] **Лоты в игре, которых нет в нашей БД** — расследование завершено (2026-07-02). Три независимые причины:
  1. **Главная (99% случаев): watchlist-центричная архитектура.** `collect_all_active_lots` (`collectors.py:58`) берёт только `(item_id, region)` из `user_watchlist`. Если ни один пользователь не добавил предмет в избранное — его лоты никогда не собираются. В `master_items` есть 2236 предметов, но коллектор видит только те, что кто-то добавил в вотчлист. Это архитектурное решение, не баг.
  2. **Вторичная: пагинация (1-5%).** Для предметов в вотчлисте мы берём первые 200 лотов от API (неизвестная дефолтная сортировка), затем сортируем по цене сами (`collectors.py:353`). Лоты за пределами 200 от API — невидимы.
  3. **Community report об API:** "показывает ~50% реальных лотов" — не верифицировано, требует ручной проверки (сравнить count через API vs ingame для конкретного предмета).
  - **Потенциальное решение:** фоновый сбор всех предметов из `master_items` с низкой частотой (не каждые 20 сек, а раз в час) — даст глобальную картину рынка без зависимости от watchlist. Нужно оценить нагрузку: 2236 предметов × 2 токена = 4472 токена/час = 74.5 запросов/мин (18.6% от лимита, в сумме с текущим ~32%).


### Внешняя информация

- Python-обёртка `stalcraft-api` v2.1.2 (PyPI, май 2026) — может содержать недокументированные эндпоинты, стоит проверить.
- Community report: "API не показывает ~половину реальных лотов" — требует верификации (сравнить count через API vs ingame).
- Неофициальный внутренний API задокументирован в `Art3mLapa/unofficial-stalcraft-api` (launcher, backend, CDN) — не для нашего use case.

---

## Идеи на будущее

- Уведомления в реальном времени — WebSocket push.
- Сравнение регионов — один товар, несколько регионов рядом.
- При параллельной работе нескольких сессий Claude Code в одном репозитории —
  после любой деструктивной git-операции (filter-repo, reset --hard, force-push)
  в ОДНОЙ сессии явно сверять `git log`/`git status`/`git diff` в ОСТАЛЬНЫХ
  открытых сессиях: рабочая копия может сброситься и молча потерять
  uncommitted-правки, сделанные параллельно.

---

## Архив

- Полная история закрытых задач и редизайна фронтенда → `docs/CHANGELOG.md`
- Деплой и инфраструктура (VPS, Caddy, первый запуск) → `docs/DEPLOY.md`
