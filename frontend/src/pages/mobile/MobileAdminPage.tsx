import AdminPage from '../AdminPage'

// Админ (мобайл). Раздел только для администратора и редко используется с
// телефона. Десктопный AdminPage не даёт горизонтального скролла страницы:
// широкая таблица пользователей обёрнута в overflowX:auto (скроллится внутри
// своего контейнера), статус-строки — grid repeat(N,1fr) (сжимаются по ширине),
// панели — flexWrap. На узком экране плотно, но работоспособно и без «уезжания»
// формы за край. Bespoke-переверстка в DCard — при необходимости позже.
export default function MobileAdminPage() {
  return <AdminPage />
}
