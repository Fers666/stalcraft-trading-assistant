import NewsPage from '../NewsPage'

// Новости (мобайл). Десктопный NewsPage — одноколоночный список карточек + форма
// (админ), без широких таблиц и фиксированных ширин → корректно ложится на узкий
// экран без горизонтального скролла. Отдельный компонент нужен как точка
// мобильного роута ModeSwitch; переверстка в bespoke-Терминал — при желании позже.
export default function MobileNewsPage() {
  return <NewsPage />
}
