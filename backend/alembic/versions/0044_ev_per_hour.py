"""feed_lots.ev_per_hour: ожидаемая прибыль в час (P0-3)

Revision ID: 0044
Revises: 0043
Create Date: 2026-08-17

Лента сортировалась по profit_total, то есть по величине «сколько заработаю,
ЕСЛИ продам». Замер после фазы B показал, что умолчание систематически поднимает
наверх то, что не продастся: 77.3 % строк ленты имеют P(продан <= 6 ч) < 40 %, и
именно у них средняя прибыль вдвое выше остальных (docs/tasks/ev-ranking.md §1).
Связь структурная — забрать большую маржу можно только продавая близко к опоре,
а это глубоко в стакане.

ev_per_hour = profit_total / est_sell_hours * P(продан <= 6 ч)

Материализуется по той же причине, что и profit_per_hour_total: сортировка
обязана идти по ПОКАЗАННОМУ числу. Сортировка по невидимой величине уже давала
здесь баг — profit_per_hour считался на единицу, а печаталась прибыль всего
лота, и порядок выдачи инвертировался.

NULL — рабочее состояние: у строки нет измеренной вероятности (страта ниже
MIN_STRATUM_N либо sale_survival ещё пуста). Такие строки уходят в конец выдачи
через nulls_last(). Подставлять p = 1 нельзя: это ровно то допущение «продастся
обязательно», от которого уходим.
"""
from alembic import op
import sqlalchemy as sa

revision = "0044"
down_revision = "0043"
branch_labels = None
depends_on = None


def upgrade():
    op.add_column("feed_lots", sa.Column("ev_per_hour", sa.Numeric(14, 2)))


def downgrade():
    # Безопасно: величина производная, пересчитывается ближайшим свипом ленты.
    op.drop_column("feed_lots", "ev_per_hour")
