"""feed_lots.tier_used: тир продажи, по которому лот прошёл в ленту

Revision ID: 0048
Revises: 0047
Create Date: 2026-08-21

До этой миграции допуск лота в ленту решался ровно одним сценарием продажи —
тиром fast (ref * 0.94). Лот дешевле опоры, но дороже ref * 0.893, отбрасывался
целиком, вместе со всеми своими premium-колонками, которые для него даже не
считались. Замер прода 2026-08-21 на 34 241 наблюдении: fast допускает 1 909
лотов в сутки, normal 3 565, premium 8 046 — то есть отбрасывалось вчетверо
больше, чем показывалось. По оружию разрыв предельный: 4 лота против 521.

Теперь допуск идёт по всем трём тирам, и строка обязана нести тот, по которому
прошла: цена тира это ещё и вероятность продажи. Замер за 7 дней по 87 тыс.
закрытых наблюдений (withdrawn считается непроданным): цена fast продаётся в
81.3 % случаев, normal в 74.5 %, premium в 49.0 %. Без метки строка тира
premium была бы неотличима от строки тира fast с той же прибылью, а сбывается
она вдвое реже.

Значение — САМЫЙ БЫСТРЫЙ из подошедших тиров (pricing.TIER_ORDER), поэтому
существующие строки ленты сохраняют свои числа и получают ровно 'fast':
изменение строго расширяющее.

Бэкфилл не нужен и server_default='fast' по сути формальность: feed_lots —
снимок, полностью перезаписываемый каждую минуту циклом collect_artifact_lots.
Умолчание стоит только ради безопасного окна между миграцией и запуском нового
образа.

ТЗ: docs/tasks/feed-multi-tier-admission.md
"""
from alembic import op
import sqlalchemy as sa

revision = "0048"
down_revision = "0047"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "feed_lots",
        sa.Column("tier_used", sa.String(10), nullable=False, server_default="fast"),
    )


def downgrade() -> None:
    op.drop_column("feed_lots", "tier_used")
